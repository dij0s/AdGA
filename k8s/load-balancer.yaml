apiVersion: v1
kind: Service
metadata:
  name: load-balancer
  namespace: isc3
spec:
  selector:
    app: sim
  type: LoadBalancer
  ports:
    - name: api
      protocol: TCP
      port: 80
      targetPort: 5000